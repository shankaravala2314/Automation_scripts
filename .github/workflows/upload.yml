name: S3 Upload Automation

on:
  push:
    branches:
      - main

jobs:
  upload-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Show Files
        run: ls -la

      - name: Print Message
        run: echo "GitHub Actions is working!"